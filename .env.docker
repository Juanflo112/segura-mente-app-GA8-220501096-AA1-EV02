# Configuración de Docker Compose para Segura-Mente App
# Este archivo contiene las variables de entorno para el despliegue local

# === Base de Datos MySQL ===
MYSQL_ROOT_PASSWORD=root_password_123
MYSQL_DATABASE=seguramente_db
MYSQL_USER=seguramente_user
MYSQL_PASSWORD=seguramente_pass_123

# === Backend Node.js ===
NODE_ENV=production
PORT=5000
DB_HOST=mysql
DB_USER=seguramente_user
DB_PASSWORD=seguramente_pass_123
DB_NAME=seguramente_db
DB_PORT=3306

# === JWT y Seguridad ===
# IMPORTANTE: Cambia este secreto en producción por uno único y seguro
JWT_SECRET=your_super_secret_jwt_key_change_in_production_2024

# === Configuración de Email (Opcional) ===
# Si deseas probar el envío de emails, configura estos valores
# Para Gmail, necesitas una "contraseña de aplicación"
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_email_app_password
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587

# === Notas de Configuración ===
# - Los valores aquí son para desarrollo/pruebas locales
# - NO uses estas credenciales en producción
# - MySQL estará disponible en localhost:3307
# - Backend estará disponible en localhost:5000
